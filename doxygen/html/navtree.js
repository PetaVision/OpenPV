var NAVTREE =
[
  [ "PetaVision", "index.html", [
    [ "Data Structures", null, [
      [ "Data Structures", "annotated.html", "annotated" ],
      [ "Data Structure Index", "classes.html", null ],
      [ "Class Hierarchy", "hierarchy.html", "hierarchy" ],
      [ "Data Fields", "functions.html", [
        [ "All", "functions.html", "functions_dup" ],
        [ "Functions", "functions_func.html", "functions_func" ]
      ] ]
    ] ]
  ] ]
];

var NAVTREEINDEX =
[
".html",
"classPV_1_1ANNErrorLayer.html#adc8eb82047d6560609b505740042e869",
"classPV_1_1ANNLayer.html#ad0951f38f916ad74fc94f5b79cdeaad9",
"classPV_1_1ANNNormalizedErrorLayer.html#ac8734a103527d47d770e2e1f2c40c472",
"classPV_1_1ANNSquaredLayer.html#abc79d6b03883e6fa9c30bc7892c962ae",
"classPV_1_1ANNWhitenedLayer.html#aa802ff6b9eb790c054283c45983193b0",
"classPV_1_1BIDSCloneLayer.html#aa3a0002b3942f42cbb439d5429784dbf",
"classPV_1_1BIDSConn.html#a7a2ca1a03366b76b3dd84fd08944d5e7",
"classPV_1_1BIDSLayer.html#a2da0b5f09f39f6631c5c9f6588d692fe",
"classPV_1_1BIDSMovieCloneMap.html#a14db26e1a3c545c00bb6b797baab84a0",
"classPV_1_1BIDSSensorLayer.html#a05ad2c3f8169a1ddca33e5fe6859afd1",
"classPV_1_1BIDSSensorLayer.html#ab109f1c96ecced0d25d4aab061cd9f25",
"classPV_1_1BaseHyPerConnProbe.html#ad2666829b95ae4d17b5ed55e5d3eb9db",
"classPV_1_1BinningLayer.html#ab8a2312dc16b736d4959442ca0997136",
"classPV_1_1CloneConn.html#a7eecf5321dc03a7c38c0091810f84503",
"classPV_1_1CloneKernelConn.html#a2cf9bfd2127468e658735459b11bd282",
"classPV_1_1CloneKernelConn.html#ae5dd03ffa594fbbef3ef97748886be9e",
"classPV_1_1CloneVLayer.html#ad99fa2c6c292783aab31c4579062704c",
"classPV_1_1ConstantLayer.html#a4656172032b7f8af79f121eb08bce740",
"classPV_1_1CopyConn.html#a36986de697e5eecda09a55abcbc0af1a",
"classPV_1_1CopyConn.html#aeb6beb94c07be8e85d9b1535ebe09c8a",
"classPV_1_1CreateMovies.html#a8494aa2f572b2a86bee9cc374ef33d76",
"classPV_1_1FeedbackConn.html#a36986de697e5eecda09a55abcbc0af1a",
"classPV_1_1FeedbackConn.html#aeb6beb94c07be8e85d9b1535ebe09c8a",
"classPV_1_1FilenameParsingGroundTruthLayer.html#ad0951f38f916ad74fc94f5b79cdeaad9",
"classPV_1_1GapConn.html#a96fc901eafe01de6b82d52e5db8833f1",
"classPV_1_1GapLayer.html#a53363a4df2f6446cdf39fdad2f2205c9",
"classPV_1_1GenerativeLayer.html#a1cd7da9d113305c40138e763f75dd4ab",
"classPV_1_1HyPerCol.html#a09e7a12f17151d5a2581e9fce67615e1",
"classPV_1_1HyPerConn.html#a16596840aabceadcca9eeeb5509bb1a2",
"classPV_1_1HyPerConn.html#acb0b03dae570319f6831254008fafa05",
"classPV_1_1HyPerLCALayer.html#a90740ae8bc9dfe6d39a9f481100dbb1a",
"classPV_1_1HyPerLayer.html#a83350c102bd6a1ffe733ab9bfee679f2",
"classPV_1_1IdentConn.html#a6f9c322107487664208f301d841f8485",
"classPV_1_1Image.html#a1cd7da9d113305c40138e763f75dd4ab",
"classPV_1_1Image.html#adb56451e0ad5d29f3482bdeca43afe5d",
"classPV_1_1ImageFromMemoryBuffer.html#a813d24a00a6a22bdd589d871776e5542",
"classPV_1_1IncrementLayer.html#a53363a4df2f6446cdf39fdad2f2205c9",
"classPV_1_1InitBIDSLateralParams.html#aed2c0c311cc4e6ed62275f15f2eb1d7c",
"classPV_1_1InitGaborWeightsParams.html#a2baff13f17470f3acfde102dff7c40cb",
"classPV_1_1InitGauss2DWeightsParams.html#aed2c0c311cc4e6ed62275f15f2eb1d7c",
"classPV_1_1InitOneToOneWeights.html#a4cb9b93ea72b6129d0e1e45e1200b1a9",
"classPV_1_1InitSmartWeights.html#ad5849346387dc966ed09aea54af87196",
"classPV_1_1InitUniformWeights.html#aa58aea2dffb596515050ca4c80031281",
"classPV_1_1KernelConn.html#a11fe8661131d8eb1de30bbaf7ee675d1",
"classPV_1_1KernelConn.html#acd4e602b7ca6223a222ff1385a6828ee",
"classPV_1_1KmeansLayer.html#a6e95809498007843832f9cd3e4aa5e6a",
"classPV_1_1LCALIFLateralConn.html#a1e39e58f356b9172319d59a53ad0a13b",
"classPV_1_1LCALIFLateralConn.html#ad1b7d44d20ba90109a548e8d79381bd8",
"classPV_1_1LCALIFLayer.html#a8548bdb747071e1dddb626545df4440a",
"classPV_1_1LIF.html#a5843eb1ee2edb3a3793fafa0d39b6b98",
"classPV_1_1LIFGap.html#a3c72b475a00be5b47aebc33e06f5c173",
"classPV_1_1LabelErrorLayer.html#a24f98bfdf6f25774d36c0904ac596ee9",
"classPV_1_1LabelLayer.html#a169e6a22d740451296076238d20646d1",
"classPV_1_1LayerFunctionProbe.html#a199288fa31a9a31479e0044d81a4c313",
"classPV_1_1LeakyIntegrator.html#a813d24a00a6a22bdd589d871776e5542",
"classPV_1_1LogLatWTAGenLayer.html#a68eb3207a3e554637b2e7a9a8e92acbe",
"classPV_1_1MLPErrorLayer.html#a1f9693acb6baeae22259a2d6d14ce438",
"classPV_1_1MLPForwardLayer.html#a054c349cdc08beb71a8834abe63fb9c0",
"classPV_1_1MLPForwardLayer.html#af853276d7a0c88a811fff224f8de8b53",
"classPV_1_1MLPOutputLayer.html#ad9e62b371e6abeaa8871e625844cd234",
"classPV_1_1MLPSigmoidLayer.html#ac96420df4bb6efc70316d7a10f676cf8",
"classPV_1_1MatchingPursuitLayer.html#ac522f7a66c0b364eb53bd2283fb3aebc",
"classPV_1_1MatchingPursuitResidual.html#aaf4a43aec7be36641aff7f6fc98f657e",
"classPV_1_1Movie.html#a5e556845dd77176e757a2f48e6ef8f77",
"classPV_1_1NormalizeContrastZeroMean.html#a41b77f7e222ed318b6f1ba384502e47a",
"classPV_1_1NormalizeSum.html#a427f0d0c931b8c4d423d82f40e10cddd",
"classPV_1_1OjaSTDPConn.html#a88cc9faa49d0761f268f80538be716c7",
"classPV_1_1PVParams.html#ad795b1d3d877c0c567c13031a076b659",
"classPV_1_1Patterns.html#a2ebefd923aa63600b89dd6da860ba2c2",
"classPV_1_1Patterns.html#ac34b058d2419ab46fc228f6cb3fa6191",
"classPV_1_1PlasticCloneConn.html#a75e8b733d9b4616d0e4c9e99ab71b226",
"classPV_1_1PointProbe.html#a30e2c7e4d3a3168b05eb9a5e644e96b4",
"classPV_1_1PoolingANNLayer.html#ac96420df4bb6efc70316d7a10f676cf8",
"classPV_1_1PoolingConn.html#a8d246349bcfd3f5188c75b8827541878",
"classPV_1_1PtwiseProductLayer.html#a3fd5a5ea382a57312f2cfaf84271f325",
"classPV_1_1RequireAllZeroActivityProbe.html#a3d3ba584c557f058f9c7c6b9d755e970",
"classPV_1_1RescaleLayer.html#ac0b4e9b89d008b554e8bbfa0380d8eda",
"classPV_1_1Retina.html#aa7c7b82680a6005ef567d948c3208d8a",
"classPV_1_1ShuffleLayer.html#a9b890401accc804413c95a3fcc11aec9",
"classPV_1_1SigmoidLayer.html#a8b26d051053778441e2b91d49d184dff",
"classPV_1_1TextStream.html#a17fd80aa3df51f1406d11bb7e4502f90",
"classPV_1_1TextStreamProbe.html#a644d69f3889920981ba54ae2c972fa8f",
"classPV_1_1TrainingLayer.html#abf42d2540cfe7e4547c88856e7057d1a",
"classPV_1_1TransposeConn.html#a827cb3830d4342a9db6221ec7411c2c9",
"classPV_1_1WTALayer.html#a392468a944d2b5be25d9ceae8851d96f",
"structIFD__.html#a0c9b11b65735cc50b6366f5a8c228f7e"
];

var SYNCONMSG = 'click to disable panel synchronisation';
var SYNCOFFMSG = 'click to enable panel synchronisation';
var navTreeSubIndices = new Array();

function getData(varName)
{
  var i = varName.lastIndexOf('/');
  var n = i>=0 ? varName.substring(i+1) : varName;
  return eval(n.replace(/\-/g,'_'));
}

function stripPath(uri)
{
  return uri.substring(uri.lastIndexOf('/')+1);
}

function stripPath2(uri)
{
  var i = uri.lastIndexOf('/');
  var s = uri.substring(i+1);
  var m = uri.substring(0,i+1).match(/\/d\w\/d\w\w\/$/);
  return m ? uri.substring(i-6) : s;
}

function localStorageSupported()
{
  try {
    return 'localStorage' in window && window['localStorage'] !== null && window.localStorage.getItem;
  }
  catch(e) {
    return false;
  }
}


function storeLink(link)
{
  if (!$("#nav-sync").hasClass('sync') && localStorageSupported()) {
      window.localStorage.setItem('navpath',link);
  }
}

function deleteLink()
{
  if (localStorageSupported()) {
    window.localStorage.setItem('navpath','');
  } 
}

function cachedLink()
{
  if (localStorageSupported()) {
    return window.localStorage.getItem('navpath');
  } else {
    return '';
  }
}

function getScript(scriptName,func,show)
{
  var head = document.getElementsByTagName("head")[0]; 
  var script = document.createElement('script');
  script.id = scriptName;
  script.type = 'text/javascript';
  script.onload = func; 
  script.src = scriptName+'.js'; 
  if ($.browser.msie && $.browser.version<=8) { 
    // script.onload does work with older versions of IE
    script.onreadystatechange = function() {
      if (script.readyState=='complete' || script.readyState=='loaded') { 
        func(); if (show) showRoot(); 
      }
    }
  }
  head.appendChild(script); 
}

function createIndent(o,domNode,node,level)
{
  if (node.parentNode && node.parentNode.parentNode) {
    createIndent(o,domNode,node.parentNode,level+1);
  }
  var imgNode = document.createElement("img");
  imgNode.width = 16;
  imgNode.height = 22;
  if (level==0 && node.childrenData) {
    node.plus_img = imgNode;
    node.expandToggle = document.createElement("a");
    node.expandToggle.href = "javascript:void(0)";
    node.expandToggle.onclick = function() {
      if (node.expanded) {
        $(node.getChildrenUL()).slideUp("fast");
        if (node.isLast) {
          node.plus_img.src = node.relpath+"ftv2plastnode.png";
        } else {
          node.plus_img.src = node.relpath+"ftv2pnode.png";
        }
        node.expanded = false;
      } else {
        expandNode(o, node, false, false);
      }
    }
    node.expandToggle.appendChild(imgNode);
    domNode.appendChild(node.expandToggle);
  } else {
    domNode.appendChild(imgNode);
  }
  if (level==0) {
    if (node.isLast) {
      if (node.childrenData) {
        imgNode.src = node.relpath+"ftv2plastnode.png";
      } else {
        imgNode.src = node.relpath+"ftv2lastnode.png";
        domNode.appendChild(imgNode);
      }
    } else {
      if (node.childrenData) {
        imgNode.src = node.relpath+"ftv2pnode.png";
      } else {
        imgNode.src = node.relpath+"ftv2node.png";
        domNode.appendChild(imgNode);
      }
    }
  } else {
    if (node.isLast) {
      imgNode.src = node.relpath+"ftv2blank.png";
    } else {
      imgNode.src = node.relpath+"ftv2vertline.png";
    }
  }
  imgNode.border = "0";
}

function newNode(o, po, text, link, childrenData, lastNode)
{
  var node = new Object();
  node.children = Array();
  node.childrenData = childrenData;
  node.depth = po.depth + 1;
  node.relpath = po.relpath;
  node.isLast = lastNode;

  node.li = document.createElement("li");
  po.getChildrenUL().appendChild(node.li);
  node.parentNode = po;

  node.itemDiv = document.createElement("div");
  node.itemDiv.className = "item";

  node.labelSpan = document.createElement("span");
  node.labelSpan.className = "label";

  createIndent(o,node.itemDiv,node,0);
  node.itemDiv.appendChild(node.labelSpan);
  node.li.appendChild(node.itemDiv);

  var a = document.createElement("a");
  node.labelSpan.appendChild(a);
  node.label = document.createTextNode(text);
  node.expanded = false;
  a.appendChild(node.label);
  if (link) {
    var url;
    if (link.substring(0,1)=='^') {
      url = link.substring(1);
      link = url;
    } else {
      url = node.relpath+link;
    }
    a.className = stripPath(link.replace('#',':'));
    if (link.indexOf('#')!=-1) {
      var aname = '#'+link.split('#')[1];
      var srcPage = stripPath($(location).attr('pathname'));
      var targetPage = stripPath(link.split('#')[0]);
      a.href = srcPage!=targetPage ? url : '#';
      a.onclick = function(){
        storeLink(link);
        if (!$(a).parent().parent().hasClass('selected'))
        {
          $('.item').removeClass('selected');
          $('.item').removeAttr('id');
          $(a).parent().parent().addClass('selected');
          $(a).parent().parent().attr('id','selected');
        }
        var pos, anchor = $(aname), docContent = $('#doc-content');
        if (anchor.parent().attr('class')=='memItemLeft') {
          pos = anchor.parent().position().top;
        } else if (anchor.position()) {
          pos = anchor.position().top;
        }
        if (pos) {
          var dist = Math.abs(Math.min(
                     pos-docContent.offset().top,
                     docContent[0].scrollHeight-
                     docContent.height()-docContent.scrollTop()));
          docContent.animate({
            scrollTop: pos + docContent.scrollTop() - docContent.offset().top
          },Math.max(50,Math.min(500,dist)),function(){
            window.location.replace(aname);
          });
        }
      };
    } else {
      a.href = url;
      a.onclick = function() { storeLink(link); }
    }
  } else {
    if (childrenData != null) 
    {
      a.className = "nolink";
      a.href = "javascript:void(0)";
      a.onclick = node.expandToggle.onclick;
    }
  }

  node.childrenUL = null;
  node.getChildrenUL = function() {
    if (!node.childrenUL) {
      node.childrenUL = document.createElement("ul");
      node.childrenUL.className = "children_ul";
      node.childrenUL.style.display = "none";
      node.li.appendChild(node.childrenUL);
    }
    return node.childrenUL;
  };

  return node;
}

function showRoot()
{
  var headerHeight = $("#top").height();
  var footerHeight = $("#nav-path").height();
  var windowHeight = $(window).height() - headerHeight - footerHeight;
  (function (){ // retry until we can scroll to the selected item
    try {
      var navtree=$('#nav-tree');
      navtree.scrollTo('#selected',0,{offset:-windowHeight/2});
    } catch (err) {
      setTimeout(arguments.callee, 0);
    }
  })();
}

function expandNode(o, node, imm, showRoot)
{
  if (node.childrenData && !node.expanded) {
    if (typeof(node.childrenData)==='string') {
      var varName    = node.childrenData;
      getScript(node.relpath+varName,function(){
        node.childrenData = getData(varName);
        expandNode(o, node, imm, showRoot);
      }, showRoot);
    } else {
      if (!node.childrenVisited) {
        getNode(o, node);
      } if (imm || ($.browser.msie && $.browser.version>8)) { 
        // somehow slideDown jumps to the start of tree for IE9 :-(
        $(node.getChildrenUL()).show();
      } else {
        $(node.getChildrenUL()).slideDown("fast");
      }
      if (node.isLast) {
        node.plus_img.src = node.relpath+"ftv2mlastnode.png";
      } else {
        node.plus_img.src = node.relpath+"ftv2mnode.png";
      }
      node.expanded = true;
    }
  }
}

function glowEffect(n,duration)
{
  n.addClass('glow').delay(duration).queue(function(next){
    $(this).removeClass('glow');next();
  });
}

function highlightAnchor()
{
  var anchor = $($(location).attr('hash'));
  if (anchor.parent().attr('class')=='memItemLeft'){
    var rows = $('.memberdecls tr[class$="'+
               window.location.hash.substring(1)+'"]');
    glowEffect(rows.children(),300); // member without details
  } else if (anchor.parents().slice(2).prop('tagName')=='TR') {
    glowEffect(anchor.parents('div.memitem'),1000); // enum value
  } else if (anchor.parent().attr('class')=='fieldtype'){
    glowEffect(anchor.parent().parent(),1000); // struct field
  } else if (anchor.parent().is(":header")) {
    glowEffect(anchor.parent(),1000); // section header
  } else {
    glowEffect(anchor.next(),1000); // normal member
  }
}

function selectAndHighlight(hash,n)
{
  var a;
  if (hash) {
    var link=stripPath($(location).attr('pathname'))+':'+hash.substring(1);
    a=$('.item a[class$="'+link+'"]');
  }
  if (a && a.length) {
    a.parent().parent().addClass('selected');
    a.parent().parent().attr('id','selected');
    highlightAnchor();
  } else if (n) {
    $(n.itemDiv).addClass('selected');
    $(n.itemDiv).attr('id','selected');
  }
  showRoot();
}

function showNode(o, node, index, hash)
{
  if (node && node.childrenData) {
    if (typeof(node.childrenData)==='string') {
      var varName    = node.childrenData;
      getScript(node.relpath+varName,function(){
        node.childrenData = getData(varName);
        showNode(o,node,index,hash);
      },true);
    } else {
      if (!node.childrenVisited) {
        getNode(o, node);
      }
      $(node.getChildrenUL()).show();
      if (node.isLast) {
        node.plus_img.src = node.relpath+"ftv2mlastnode.png";
      } else {
        node.plus_img.src = node.relpath+"ftv2mnode.png";
      }
      node.expanded = true;
      var n = node.children[o.breadcrumbs[index]];
      if (index+1<o.breadcrumbs.length) {
        showNode(o,n,index+1,hash);
      } else {
        if (typeof(n.childrenData)==='string') {
          var varName = n.childrenData;
          getScript(n.relpath+varName,function(){
            n.childrenData = getData(varName);
            node.expanded=false;
            showNode(o,node,index,hash); // retry with child node expanded
          },true);
        } else {
          var rootBase = stripPath(o.toroot.replace(/\..+$/, ''));
          if (rootBase=="index" || rootBase=="pages") {
            expandNode(o, n, true, true);
          }
          selectAndHighlight(hash,n);
        }
      }
    }
  } else {
    selectAndHighlight(hash);
  }
}

function getNode(o, po)
{
  po.childrenVisited = true;
  var l = po.childrenData.length-1;
  for (var i in po.childrenData) {
    var nodeData = po.childrenData[i];
    po.children[i] = newNode(o, po, nodeData[0], nodeData[1], nodeData[2],
      i==l);
  }
}

function gotoNode(o,subIndex,root,hash,relpath)
{
  var nti = navTreeSubIndices[subIndex][root+hash];
  o.breadcrumbs = $.extend(true, [], nti ? nti : navTreeSubIndices[subIndex][root]);
  if (!o.breadcrumbs && root!=NAVTREE[0][1]) { // fallback: show index
    navTo(o,NAVTREE[0][1],"",relpath);
    $('.item').removeClass('selected');
    $('.item').removeAttr('id');
  }
  if (o.breadcrumbs) {
    o.breadcrumbs.unshift(0); // add 0 for root node
    showNode(o, o.node, 0, hash);
  }
}

function navTo(o,root,hash,relpath)
{
  var link = cachedLink();
  if (link) {
    var parts = link.split('#');
    root = parts[0];
    if (parts.length>1) hash = '#'+parts[1];
    else hash='';
  }
  if (root==NAVTREE[0][1]) {
    $('#nav-sync').css('top','30px');
  } else {
    $('#nav-sync').css('top','5px');
  }
  if (hash.match(/^#l\d+$/)) {
    var anchor=$('a[name='+hash.substring(1)+']');
    glowEffect(anchor.parent(),1000); // line number
    hash=''; // strip line number anchors
    //root=root.replace(/_source\./,'.'); // source link to doc link
  }
  var url=root+hash;
  var i=-1;
  while (NAVTREEINDEX[i+1]<=url) i++;
  if (navTreeSubIndices[i]) {
    gotoNode(o,i,root,hash,relpath)
  } else {
    getScript(relpath+'navtreeindex'+i,function(){
      navTreeSubIndices[i] = eval('NAVTREEINDEX'+i);
      if (navTreeSubIndices[i]) {
        gotoNode(o,i,root,hash,relpath);
      }
    },true);
  }
}

function showSyncOff(n,relpath)
{
    n.html('<img src="'+relpath+'sync_off.png" title="'+SYNCOFFMSG+'"/>');
}

function showSyncOn(n,relpath)
{
    n.html('<img src="'+relpath+'sync_on.png"/ title="'+SYNCONMSG+'">');
}

function toggleSyncButton(relpath)
{
  var navSync = $('#nav-sync');
  if (navSync.hasClass('sync')) {
    navSync.removeClass('sync');
    showSyncOff(navSync,relpath);
    storeLink(stripPath2($(location).attr('pathname'))+$(location).attr('hash'));
  } else {
    navSync.addClass('sync');
    showSyncOn(navSync,relpath);
    deleteLink();
  }
}

function initNavTree(toroot,relpath)
{
  var o = new Object();
  o.toroot = toroot;
  o.node = new Object();
  o.node.li = document.getElementById("nav-tree-contents");
  o.node.childrenData = NAVTREE;
  o.node.children = new Array();
  o.node.childrenUL = document.createElement("ul");
  o.node.getChildrenUL = function() { return o.node.childrenUL; };
  o.node.li.appendChild(o.node.childrenUL);
  o.node.depth = 0;
  o.node.relpath = relpath;
  o.node.expanded = false;
  o.node.isLast = true;
  o.node.plus_img = document.createElement("img");
  o.node.plus_img.src = relpath+"ftv2pnode.png";
  o.node.plus_img.width = 16;
  o.node.plus_img.height = 22;

  if (localStorageSupported()) {
    var navSync = $('#nav-sync');
    if (cachedLink()) {
      showSyncOff(navSync,relpath);
      navSync.removeClass('sync');
    } else {
      showSyncOn(navSync,relpath);
    }
    navSync.click(function(){ toggleSyncButton(relpath); });
  }

  navTo(o,toroot,window.location.hash,relpath);

  $(window).bind('hashchange', function(){
     if (window.location.hash && window.location.hash.length>1){
       var a;
       if ($(location).attr('hash')){
         var clslink=stripPath($(location).attr('pathname'))+':'+
                               $(location).attr('hash').substring(1);
         a=$('.item a[class$="'+clslink+'"]');
       }
       if (a==null || !$(a).parent().parent().hasClass('selected')){
         $('.item').removeClass('selected');
         $('.item').removeAttr('id');
       }
       var link=stripPath2($(location).attr('pathname'));
       navTo(o,link,$(location).attr('hash'),relpath);
     }
  })

  $(window).load(showRoot);
}

