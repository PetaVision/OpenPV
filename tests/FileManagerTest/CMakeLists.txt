set(SRC_CPP
  src/FileManagerTest.cpp
)

if (PV_SYSTEM_TEST_MPIBLOCK)
  pv_add_test(NO_PARAMS MPI_ONLY MIN_MPI_COPIES 32 MAX_MPI_COPIES 32 SRCFILES ${SRC_CPP} ${SRC_HPP} ${SRC_C} ${SRC_H})
endif (PV_SYSTEM_TEST_MPIBLOCK)
