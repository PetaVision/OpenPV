set(SRC_CPP
  src/WeightDecayBroadcastTest.cpp
)

pv_add_test(PARAMS weightL2Decay SRCFILES ${SRC_CPP} ${SRC_HPP} ${SRC_C} ${SRC_H})

get_filename_component(BASE_NAME ${CMAKE_CURRENT_SOURCE_DIR} NAME)
set(BATCHTEST "${BASE_NAME}_2batch")
set(TEST_BINARY "${CMAKE_CURRENT_BINARY_DIR}/${CMAKE_BUILD_TYPE}/${BASE_NAME}")
set(TEST_FLAGS "-t;${PV_SYSTEM_TEST_THREADS}")
set(TEST_PARAMS "input/weightL2Decay.params")
set(TEST_LOG_DIR ${CMAKE_CURRENT_BINARY_DIR})
set(TEST_LOG_FILE ${TEST_LOG_DIR}/${BATCHTEST}.log)
add_test(NAME ${BATCHTEST} WORKING_DIRECTORY ${CMAKE_CURRENT_BINARY_DIR}
   COMMAND ${MPIEXEC} ${MPIEXEC_NUMPROC_FLAG} 2 ${PV_MPI_OPTIONS_EXTRA} ${MPIEXEC_PREFLAGS} ${PV_SYSTEM_TEST_COMMAND} ${TEST_BINARY} ${TEST_FLAGS} -batchwidth 2 -p ${TEST_PARAMS} -l ${TEST_LOG_FILE})
