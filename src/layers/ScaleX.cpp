// There is a layer type ScaleXLayer.
// When the column sees a param group with keyword ScaleXLayer,
// it creates a LinearTransformLayer. The code for LinearTransformLayer
// picks up the group keyword and creates the appropriate activity buffer.
