project(PetaVision)

# See http://www.cmake.org/Wiki/CMake_Useful_Variables for more variables to set
cmake_minimum_required(VERSION 2.6)

# The user may specify a non-standard compiler name for MPI
#  For example, the user may type:
#    cmake cMakeLists.txt -DMPI_C_COMPILER_NAME:STRING=openmpicc -DMPI_CXX_COMPILER_NAME:STRING=openmpic++
#    

set(MPI_C_COMPILER_NAME "mpicc" CACHE STRING "mpicc")
set(MPI_CXX_COMPILER_NAME "mpic++" CACHE STRING "mpic++")


add_subdirectory(PetaVision)
add_subdirectory(BIDS)
