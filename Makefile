#
# This file will make the PetaVision library, libpv.a in the src/build directory
#    - another choice is to use Eclipse and checkout PetaVision as a project
#

all:
	cd src;	make all;

clean:
	cd src; make clean;
